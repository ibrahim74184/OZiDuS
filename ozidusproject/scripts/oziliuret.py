from django.db import models
"""from ozildata.models import ZilMelodi


def uret():
    gziller = ZilMelodi.objects.all.filter(active=True)
    for i in range(len(gziller)):
        for x, y in gziller[i].items():
            print(f"{x}, {y}")
"""